esphome:
  name: improv-serial-esphome-device

# esp8266:
#   board: d1_mini

esp32:
  board: m5stack-atom

logger:

web_server:
  local: true

wifi:
  ap:
captive_portal:
improv_serial:

button:
  - platform: factory_reset
    name: Reset to Factory Default Settings
